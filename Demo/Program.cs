namespace Demo
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region Video01
            //Car C1;

            // C1 = new Car(123 , "Kia Cerato 2021" , 220);
            // C1 = new Car(888 , "Renault Megane 2022");
            // C1 = new Car(555);

            /// new 
            /// 1. Allocate required number of bytes for the allocated object 
            /// 2.initialize the allocated bytes for each and every attribute with default value
            /// 3.call user-defined constructor
            /// 4. assign the address to the reference C2
            /// 

            //Console.WriteLine(C1.ToString()); 
            #endregion
        }
    }
}
